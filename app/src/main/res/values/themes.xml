<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Calculator" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
<!--         <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="tint">@color/white</item>
    </style>

    <style name="Theme.Calculator" parent="Base.Theme.Calculator" />
    <style name="btnStyle" parent="Widget.AppCompat.Button">
        <item name="android:textSize" >50dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/btn_shape</item>
    </style>
    <style name="btnStyle1" parent="Widget.AppCompat.Button">
        <item name="android:textSize" >30dp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:background">@drawable/btn_shape1</item>
    </style>
    <style name="btnStyle2" parent="Widget.AppCompat.Button">
        <item name="android:textSize" >30dp</item>
        <item name="android:textColor">@color/darkblue</item>
        <item name="android:background">@drawable/btn_shape2</item>
    </style>
</resources>